name: Build DLL

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      STEAM_INSTALL_DIR: /home/runner/.steam/steam

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Prepare fake Steam path
      run: |
        mkdir -p /tmp/steam/config
        echo '"path" "/tmp/fake_steam"' > /tmp/steam/config/libraryfolders.vdf
        mkdir -p /tmp/fake_steam/steamapps/common/PlateUp/PlateUp

    - name: Restore project
      run: dotnet restore MuffinUp.csproj

    - name: Build DLL
      run: |
        dotnet build MuffinUp.csproj --configuration Release --no-restore \
          /p:SteamInstallDirectory=/tmp/steam \
          /p:GamePath=/tmp/fake_steam/steamapps/common/PlateUp/PlateUp \
          /p:EnableModDeployLocal=false \
          /p:EnableGameDebugging=false \
          /p:EnableAutoWorkshopReferences=false \
          /p:EnableAutoLocalModsReferences=false

    - name: Upload DLL artifact
      uses: actions/upload-artifact@v4
      with:
        name: MuffinUp-DLL
        path: MuffinUp/bin/Release/net472/*.dll
